import Component from "$store/components/ui/MultiStepForm.tsx";
import type { Props } from "$store/components/ui/MultiStepForm.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;